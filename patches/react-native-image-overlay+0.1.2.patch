diff --git a/node_modules/react-native-image-overlay/index.js b/node_modules/react-native-image-overlay/index.js
index bb6cac1..46e4fe8 100644
--- a/node_modules/react-native-image-overlay/index.js
+++ b/node_modules/react-native-image-overlay/index.js
@@ -6,12 +6,27 @@ import {
   StyleSheet,
   Text,
   View,
-  Image,
-  ViewPropTypes
 } from "react-native";
 
 const { width } = Dimensions.get("window");
 
+const imageSourcePropType = PropTypes.oneOfType([
+  PropTypes.number,
+  PropTypes.shape({ uri: PropTypes.string }),
+  PropTypes.arrayOf(
+    PropTypes.oneOfType([
+      PropTypes.number,
+      PropTypes.shape({ uri: PropTypes.string }),
+    ]),
+  ),
+]);
+
+const stylePropType = PropTypes.oneOfType([
+  PropTypes.object,
+  PropTypes.array,
+  PropTypes.number,
+]);
+
 export default class ImageOverlay extends Component {
   render() {
     const {
@@ -83,14 +98,14 @@ const styles = StyleSheet.create({
 
 ImageOverlay.propTypes = {
   rounded: PropTypes.number,
-  source: Image.propTypes.source,
+  source: imageSourcePropType,
   height: PropTypes.number,
   title: PropTypes.string,
-  titleStyle: Text.propTypes.style,
+  titleStyle: stylePropType,
   overlayColor: PropTypes.string,
   overlayAlpha: PropTypes.number,
   contentPosition: PropTypes.oneOf(["top", "center", "bottom"]),
-  containerStyle: ViewPropTypes.style,
+  containerStyle: stylePropType,
   blurRadius: PropTypes.number,
   children: PropTypes.element
 };
